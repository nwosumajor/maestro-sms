# Workflow Rbac Docker

> Integration of the big spec — richer RBAC, Approval Workflow Engine, Docker/Compose/Nginx (decisions + status)

*Engineering note (project) — a point-in-time record from a build session. The durable spec is [CLAUDE.md](../../CLAUDE.md); verify details against the code before relying on them.*

---

User dropped a sweeping "Principal DevOps Architect" spec (polyrepo, Mongo, approval engine, Docker, etc.). Reviewed it as INTEGRATION (most already existed). DECISIONS made (via AskUserQuestion): keep MONOREPO (not polyrepo); LMS flexible content in POSTGRES JSONB (NOT MongoDB — one RLS isolation model); BUILD now: richer RBAC + Approval Engine + Docker/Compose. (Flagged the CLAUDE.md conflicts: monorepo, no-Mongo-in-stack, approval-engine-was-stubbed.)

Built + verified:
1. **Richer RBAC roles + matrix** (seed): added roles board, principal, accountant, hr_clerk (+ existing school_admin/teacher/student/parent). Encoded section-2 matrix in seed ROLE_PERMS; seed now RECONCILES role->perms (deleteMany before re-add) so it's authoritative. New demo users principal@/board@/accountant@/hr@demo.school (pw password123). school_admin no longer = all perms (restricted: no grade.write, no veto).
2. **Approval Workflow Engine** (`apps/api/src/workflow/`, `packages/db/prisma/schema/workflow.prisma`, `packages/types/src/permissions/workflow.ts`): deterministic state machine DRAFT->PENDING_REVIEW->REVISION_REQUESTED->APPROVED|REJECTED (+ board VETO APPROVED->REJECTED). `WorkflowRequest` + immutable `WorkflowAuditLog` (append-only RLS, captures old/new state, initiator, approver, comments, timestamp). Endpoints POST /workflows, /:id/{submit,review,veto}, GET /workflows[/:id]. Separation of duties (reviewer != initiator). Migration `20260619115156_workflow`, RLS `prisma/rls/05_workflow_rls.sql`. VERIFIED full lifecycle + audit trail + guards (403 self-review, 409 illegal transition). Unit spec `test/workflow/workflow.service.spec.ts`. api tests now 49, web 4.
3. **Docker/Compose/Nginx** (Part 3): `apps/api/Dockerfile` + `apps/web/Dockerfile` (multi-stage node:20-alpine, non-root `app`, web uses Next `output:standalone` + outputFileTracingRoot), `apps/api/docker-entrypoint.sh` (migrate deploy -> apply RLS -> optional seed -> start), `infrastructure/docker-compose.yml` (postgres/redis/backend/frontend/nginx on internal `appnet`; DB+redis publish NO host ports; backend depends_on service_healthy; only nginx publishes 80/443; NO mongo per decision), `infrastructure/nginx/nginx.conf` (proxies to frontend; backend never host-exposed), `infrastructure/postgres/init/01-app-role.sh` (provisions least-priv major_user on first init), `.dockerignore`, `infrastructure/.env.example`. Moved prisma/tsx/bcryptjs to @sms/db DEPS so migrate+seed work in the pruned prod image. Compose config VALIDATED; image build in progress.

Run locally: `cd infrastructure && cp .env.example .env && docker compose up --build` → app at http://localhost (nginx). Demo logins pw password123.

FULL STACK VERIFIED end-to-end (docker compose up): all 5 containers up, postgres+redis healthy, backend entrypoint auto-ran migrate→RLS→seed→listen, nginx→frontend GET / =200 + /api/auth/csrf=200, and a containerized login (backend→postgres→bcrypt→RBAC) returned principal w/ 16 perms. Container build/run GOTCHAS fixed (all only surface at `compose up`): (1) Prisma on Alpine needs `binaryTargets=["native","linux-musl-openssl-3.0.x"]` in the generator + `apk add openssl` (else "failed to detect libssl, defaulting to openssl-1.1.x" engine crash); (2) pnpm puts @sms/api direct deps in `apps/api/node_modules` — the runtime image MUST copy it (else "Cannot find module reflect-metadata"); (3) `prisma db seed` shells out to `tsx` — add node_modules/.bin to PATH in the entrypoint; (4) RLS apply is NOT idempotent (CREATE POLICY errors if exists) — entrypoint guards on policy `wal_select` and skips if present; (5) Dockerfile COPY of `apps/web/public` fails if the dir is absent — added `apps/web/public/robots.txt` + `apps/web/app/page.tsx` (root was 404). docker build needs `--network=host` (or compose `build.network: host`) for registry egress in this env. Migrate+seed run as DATABASE_MIGRATE_URL (superuser); the API server runs as DATABASE_URL (major_user). Images: sms-web ~257MB (standalone), sms-api ~1.24GB (pnpm deploy would slim it). STACK LEFT RUNNING.

REMAINING from spec (not yet built): LMS course/module/quiz/forum content (Postgres JSONB), SIS contact/medical + attendance, Finance/invoicing, Notification router, Document Vault. Still no UI for most modules. Production: secrets manager, TLS certs, remove dev x-dev-principal bypass.
